# Ollive application runtime (Python 3.11)
# vLLM is intentionally separate; see requirements-vllm.txt.
streamlit>=1.59,<2
PyYAML>=6.0,<7
python-dotenv>=1.2,<2
pydantic>=2.13,<3
openai>=2.46,<3
sentence-transformers>=5.6,<6
numpy>=2.0,<3
faiss-cpu>=1.14,<2

# Optional at runtime, but installed by default so the configured integrations
# work immediately when their environment variables are supplied.
tavily-python>=0.7,<1
langfuse>=4.14,<5
