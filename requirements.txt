# Complete local Ollive runtime (Python 3.11, Linux/NVIDIA for vLLM).
# Streamlit and vLLM run as separate processes from this one environment.
vllm==0.25.0
huggingface-hub>=1.2,<2

streamlit>=1.59,<2
PyYAML>=6.0,<7
python-dotenv>=1.2,<2
pydantic>=2.13,<3
openai>=2.46,<3
sentence-transformers>=5.6,<6
numpy>=2.0,<3
faiss-cpu>=1.14,<2

# Configured integrations. They remain inactive without their environment keys.
tavily-python>=0.7,<1
langfuse>=4.14,<5
