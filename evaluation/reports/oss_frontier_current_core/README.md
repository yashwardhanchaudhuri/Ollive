# Historical matched OSS-frontier baseline
## At a glance


| Field | Value |
|---|---|
| Source run | `evaluation/runs/oss_frontier_current_core.jsonl` |
| Scope | 72 cases per backend, one generation per case |
| Agent revision | `ebbcd4a` |
| Purpose | Prior matched baseline retained for before/after analysis |

`report.md` is generated from the raw JSONL run. `summary.json` and `assets/` preserve the historical result; they are not the current submission evidence.
